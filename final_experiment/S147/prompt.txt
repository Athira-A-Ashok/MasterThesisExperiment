Build a Todo app using React and Vite following modern frontend development patterns. State should be managed using useReducer with well-structured action types for task operations. Each task must include a unique id, text content, completion status, and creation timestamp. The UI should be component-driven with separate modules for input, task list, and individual task rendering. Improve performance by using React.memo for task items and useCallback for all event handlers. give th code without using typescript.Use useMemo to derive filtered task lists based on current filter selection instead of storing redundant.  state. Persist tasks using localStorage through a custom hook that handles both read and writeoperations. Ensure the application is fully accessible with proper semantic HTML and keyboard navigation support.